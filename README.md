# 3H252 - ALDO ALDAZ 24310322
# Animación ASCII en C++ con FTXUI
Este proyecto ha sido desarrollado en C++ utilizando la librería `FTXUI` para el renderizado en la terminal.

## 🛠️ Tecnologías Utilizadas

* **C++** (con estándar C++17)
* **Librería FTXUI** (incluida en el repositorio)
* **make** (para la compilación)

---

## ⚙️ Instrucciones de Compilación y Ejecución

Este método utiliza el `Makefile` proporcionado y es la forma más sencilla de compilar y ejecutar el proyecto.

1.  **Abrir una terminal** y navegar a la carpeta raíz del proyecto.

2.  **Compilar y Ejecutar** con un solo comando:
    ```bash
    make run
    ```

3.  **(Opcional) Compilar solamente:**
    ```bash
    make
    ```
    Esto creará un ejecutable llamado `animacion` en la carpeta `bin/`.

4.  **(Opcional) Limpiar archivos:**
    Para eliminar los archivos generados por la compilación.
    ```bash
    make clean
    ```

## ▶️ Ejecución

Si compilaste el programa usando `make run`, se ejecutará automáticamente.

Si lo compilaste manualmente o solo usaste `make`, puedes ejecutar la animación con:

```bash
./bin/animacion
```

## ALDO GAEL ALDAZ ROSALES - 24310322
